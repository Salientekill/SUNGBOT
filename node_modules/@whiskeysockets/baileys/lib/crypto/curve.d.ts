import { KeyPair } from '../Types';
export declare const generateSignalPubKey: (pubKey: Uint8Array | Buffer) => Buffer;
export declare function generateKeyPair(): {
    pubKey: Buffer;
    privKey: Buffer;
};
export declare function calculateAgreement(pubKey: Buffer, privKey: Buffer): Buffer<ArrayBufferLike>;
export declare function calculateSignature(privKey: Buffer, message: Buffer): Buffer<Uint8Array<ArrayBufferLike>>;
export declare function verifySignature(pubKey: Buffer, msg: Buffer, sig: Buffer, isInit?: boolean): boolean;
export declare const Curve: {
    generateKeyPair: () => KeyPair;
    sharedKey: (privateKey: Uint8Array, publicKey: Uint8Array) => Buffer<Buffer<ArrayBufferLike>>;
    sign: (privateKey: Uint8Array, buf: Uint8Array) => Buffer<Uint8Array<ArrayBufferLike>>;
    verify: (pubKey: Uint8Array, message: Uint8Array, signature: Uint8Array) => boolean;
};
export declare function signedKeyPair(identityKeyPair: KeyPair, keyId: number): {
    keyPair: KeyPair;
    signature: Buffer<Uint8Array<ArrayBufferLike>>;
    keyId: number;
};
