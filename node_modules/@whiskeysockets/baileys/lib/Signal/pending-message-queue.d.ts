/**
 * 🔐 PENDING MESSAGE QUEUE
 * Sistema de fila de mensagens pendentes para decriptação posterior
 * Armazena mensagens criptografadas quando o sender key não está disponível
 * Processa automaticamente quando o sender key chegar
 */
export interface PendingMessage {
    id: string;
    groupJid: string;
    participantJid: string;
    senderKeyId: string;
    rawData: string;
    timestamp: number;
    retryCount: number;
}
type MessageProcessor = (pending: PendingMessage) => Promise<boolean>;
/**
 * Adiciona uma mensagem à fila de pendentes
 */
export declare function addToPendingQueue(messageId: string, groupJid: string, participantJid: string, senderKeyId: string, encryptedData: Uint8Array): Promise<boolean>;
/**
 * Processa mensagens pendentes para um sender key específico
 * Chamado quando um novo sender key é recebido
 */
export declare function processPendingMessages(senderKeyId: string, processor: MessageProcessor): Promise<number>;
/**
 * Registra um processador de mensagens para um sender key
 */
export declare function registerMessageProcessor(senderKeyId: string, processor: MessageProcessor): void;
/**
 * Remove processador de mensagens
 */
export declare function unregisterMessageProcessor(senderKeyId: string): void;
/**
 * Obtém processador registrado
 */
export declare function getMessageProcessor(senderKeyId: string): MessageProcessor | undefined;
/**
 * Limpa mensagens antigas da fila (mais de 1 hora)
 */
export declare function cleanupOldPendingMessages(): Promise<number>;
/**
 * Obtém estatísticas da fila
 */
export declare function getPendingQueueStats(): Promise<{
    total: number;
    byGroup: Record<string, number>;
}>;
export {};
//# sourceMappingURL=pending-message-queue.d.ts.map