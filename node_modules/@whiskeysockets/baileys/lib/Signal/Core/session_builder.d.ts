import { proto } from '../../../WAProto';
import { E2ESession, SignalKeyPair, SignalSessionStore } from '../../Types/Signal';
import ProtocolAddress from './protocol_address';
import SessionRecord, { SessionEntry } from './session_record';
declare class SessionBuilder {
    addr: ProtocolAddress;
    storage: SignalSessionStore;
    constructor(storage: SignalSessionStore, protocolAddress: ProtocolAddress);
    initOutgoing(device: E2ESession): Promise<void>;
    initIncoming(sessionRecord: SessionRecord, message: proto.PreKeySignalMessage): Promise<number | null | undefined>;
    initSession(isInitiator: boolean, ourEphemeralKey: SignalKeyPair | undefined, ourSignedKey: SignalKeyPair | undefined, theirIdentityPubKey: Uint8Array, theirEphemeralPubKey: Uint8Array | undefined, theirSignedPubKey: Uint8Array | undefined, registrationId: number): Promise<SessionEntry>;
    calculateSendingRatchet(sessionEntry: SessionEntry, remoteKey: Buffer): void;
}
export default SessionBuilder;
