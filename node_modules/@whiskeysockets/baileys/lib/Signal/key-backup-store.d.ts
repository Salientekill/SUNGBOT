/**
 * 🔐 KEY BACKUP STORE
 * Sistema de backup persistente de chaves usando SQLite
 * Armazena backups de sessões e sender keys para recuperação em caso de acidente
 */
export declare class KeyBackupStore {
    private db;
    private initialized;
    private dbPath;
    constructor(sessionFolder?: string);
    /**
     * Inicializa o banco de dados de backup
     */
    init(): Promise<boolean>;
    /**
     * Salva backup de uma sessão
     */
    backupSession(sessionId: string, data: any): Promise<boolean>;
    /**
     * Recupera backup de uma sessão
     */
    getSessionBackup(sessionId: string): Promise<any | null>;
    /**
     * Remove backup de uma sessão
     */
    deleteSessionBackup(sessionId: string): Promise<void>;
    /**
     * Salva backup de uma sender key
     */
    backupSenderKey(keyId: string, data: any): Promise<boolean>;
    /**
     * Recupera backup de uma sender key
     */
    getSenderKeyBackup(keyId: string): Promise<any | null>;
    /**
     * Remove backup de uma sender key
     */
    deleteSenderKeyBackup(keyId: string): Promise<void>;
    /**
     * Retorna estatísticas do backup
     */
    getStats(): {
        sessions: number;
        senderKeys: number;
        pendingMessages: number;
        dbPath: string;
    };
    /**
     * Adiciona mensagem à fila de pendentes
     */
    addPendingMessage(pending: {
        id: string;
        groupJid: string;
        participantJid: string;
        senderKeyId: string;
        rawData: string;
        timestamp: number;
        retryCount: number;
    }): Promise<boolean>;
    /**
     * Obtém mensagens pendentes para um sender key específico
     */
    getPendingMessages(senderKeyId: string): Promise<Array<{
        id: string;
        groupJid: string;
        participantJid: string;
        senderKeyId: string;
        rawData: string;
        timestamp: number;
        retryCount: number;
    }>>;
    /**
     * Remove mensagem da fila
     */
    removePendingMessage(messageId: string): Promise<void>;
    /**
     * Limpa mensagens antigas da fila
     */
    cleanupOldPendingMessages(maxAgeMs: number): Promise<number>;
    /**
     * Obtém estatísticas da fila de pendentes
     */
    getPendingQueueStats(): Promise<{
        total: number;
        byGroup: Record<string, number>;
    }>;
    /**
     * Fecha a conexão com o banco
     */
    close(): void;
}
export declare function getKeyBackupStore(sessionFolder?: string): KeyBackupStore;
//# sourceMappingURL=key-backup-store.d.ts.map