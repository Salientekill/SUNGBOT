import { DEFAULT_CONNECTION_CONFIG } from '../Defaults/index.js';
import { makeCommunitiesSocket } from './communities.js';
// export the last socket layer
const makeWASocket = (config) => {
    const newConfig = {
        ...DEFAULT_CONNECTION_CONFIG,
        ...config
    };
    if (!newConfig.getPrivacyToken && !newConfig.storePrivacyTokens) {
        // Both functions missing - install in-memory defaults
        const memoryPrivacyTokens = new Map();
        newConfig.getPrivacyToken = async (jid) => memoryPrivacyTokens.get(jid);
        newConfig.storePrivacyTokens = async (entries) => {
            for (const entry of entries) {
                memoryPrivacyTokens.set(entry.jid, entry.token);
            }
        };
    }
    else if (!newConfig.getPrivacyToken || !newConfig.storePrivacyTokens) {
        throw new Error('Both getPrivacyToken and storePrivacyTokens must be provided together, or both must be omitted for in-memory storage. ' +
            'You cannot mix custom and default implementations.');
    }
    // If the user hasn't provided their own history sync function,
    // let's create a default one that respects the syncFullHistory flag.
    // TODO: Change
    if (config.shouldSyncHistoryMessage === undefined) {
        newConfig.shouldSyncHistoryMessage = () => !!newConfig.syncFullHistory;
    }
    return makeCommunitiesSocket(newConfig);
};
export default makeWASocket;
//# sourceMappingURL=index.js.map