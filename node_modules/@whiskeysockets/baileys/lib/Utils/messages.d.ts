import { Buffer } from 'node:buffer';
import { Readable } from 'node:stream';
import type { WasmWhatsAppClient } from '@oxidezap/whatsapp-rust-bridge';
import { type MediaType } from '../Defaults/index.js';
import type { AnyMediaMessageContent, AnyMessageContent, MessageContentGenerationOptions, MessageGenerationOptions, MessageGenerationOptionsFromContent, MessageUserReceipt, WAMessage, WAMessageContent, WAMessageKey } from '../Types/index.js';
import { WAProto } from '../Types/index.js';
import { proto } from '../WAProto/runtime.js';
import type { ILogger } from './logger.js';
import { type MediaDownloadOptions } from './messages-media.js';
type ExtractByKey<T, K extends PropertyKey> = T extends Record<K, unknown> ? T : never;
/**
 * Uses a regex to test whether the string contains a URL, and returns the URL if it does.
 * @param text eg. hello https://google.com
 * @returns the URL, eg. https://google.com
 */
export declare const extractUrlFromText: (text: string) => string | undefined;
export declare const generateLinkPreviewIfRequired: (text: string, getUrlInfo: MessageGenerationOptions['getUrlInfo'], logger: MessageGenerationOptions['logger']) => Promise<import("../index.js").WAUrlInfo | undefined>;
export declare const prepareWAMessageMedia: (message: AnyMediaMessageContent, options: MessageContentGenerationOptions) => Promise<WAProto.Message>;
export declare const prepareDisappearingMessageSettingContent: (ephemeralExpiration?: number) => WAProto.Message;
/**
 * Generate forwarded message content like WA does
 * @param message the message to forward
 * @param options.forceForward will show the message as forwarded even if it is from you
 */
export declare const generateForwardMessageContent: (message: WAMessage, forceForward?: boolean) => WAProto.IMessage;
export declare const hasNonNullishProperty: <K extends PropertyKey>(message: AnyMessageContent, key: K) => message is ExtractByKey<AnyMessageContent, K>;
export declare const generateWAMessageContent: (message: AnyMessageContent, options: MessageContentGenerationOptions) => Promise<WAProto.Message>;
export declare const generateWAMessageFromContent: (jid: string, message: WAMessageContent, options: MessageGenerationOptionsFromContent) => WAMessage;
export declare const generateWAMessage: (jid: string, content: AnyMessageContent, options: MessageGenerationOptions) => Promise<WAMessage>;
/** Get the key to access the true type of content */
export declare const getContentType: (content: proto.IMessage | undefined) => keyof WAProto.IMessage | undefined;
/**
 * Normalizes ephemeral, view once messages to regular message content
 * Eg. image messages in ephemeral messages, in view once messages etc.
 * @param content
 * @returns
 */
export declare const normalizeMessageContent: (content: WAMessageContent | null | undefined) => WAMessageContent | undefined;
/**
 * Extract the true message content from a message
 * Eg. extracts the inner message from a disappearing message/view once message
 */
export declare const extractMessageContent: (content: WAMessageContent | undefined | null) => WAMessageContent | undefined;
export type DownloadMediaMessageContext = {
    reuploadRequest: (msg: WAMessage) => Promise<WAMessage>;
    logger: ILogger;
    /** Bridge client for media download — handles CDN failover, auth refresh,
     *  HMAC-SHA256 verification, and AES-256-CBC decryption internally. */
    waClient: Pick<WasmWhatsAppClient, 'downloadMedia' | 'downloadMediaStream'>;
};
/**
 * Downloads the given message. Throws an error if it's not a media message.
 *
 * Uses the Rust bridge for download — provides CDN failover, automatic auth
 * refresh on 401/404, HMAC-SHA256 integrity verification, and AES-256-CBC
 * decryption. Requires `ctx.waClient` (the bridge client).
 */
export declare const downloadMediaMessage: <Type extends 'buffer' | 'stream'>(message: WAMessage, type: Type, options: MediaDownloadOptions, ctx: DownloadMediaMessageContext) => Promise<Type extends "buffer" ? Buffer<ArrayBufferLike> : Readable>;
export declare const _registerActiveBridgeClient: (client: WasmWhatsAppClient, logger?: ILogger) => void;
/**
 * Drop the module-level pointer when `sock.end()` frees the client it points
 * at. Without this the global keeps a strong reference to a freed
 * `WasmWhatsAppClient`, so the next standalone `downloadContentFromMessage()`
 * call reaches into a null wasm pointer and fails with an opaque
 * wasm-bindgen error instead of the actionable "no bridge client" Boom.
 *
 * Identity-checked on purpose: a multi-account host that ends socket A after
 * creating socket B must not clear B's registration.
 */
export declare const _unregisterActiveBridgeClient: (client: WasmWhatsAppClient) => void;
/**
 * Upstream-Baileys-compatible standalone media download. Builds a synthetic
 * `WAMessage` from the supplied media subcontent (image/video/audio/etc fields)
 * and routes it through {@link downloadMediaMessage}, so CDN failover, auth
 * refresh, HMAC verification, and AES-256-CBC decryption all happen inside the
 * Rust bridge — same code path as `sock.downloadMedia`.
 *
 * The bridge client is required. Pass it explicitly via `opts.client` for
 * multi-account hosts; otherwise the helper falls back to the most-recently
 * created bridge client (registered automatically by `makeWASocket`).
 */
export declare const downloadContentFromMessage: (mediaContent: {
    directPath?: string | null;
    mediaKey?: Uint8Array | null;
    fileSha256?: Uint8Array | null;
    fileEncSha256?: Uint8Array | null;
    fileLength?: number | Long | null;
    url?: string | null;
}, type: MediaType, opts?: MediaDownloadOptions & {
    client?: WasmWhatsAppClient;
    logger?: ILogger;
}) => Promise<Readable>;
type Long = {
    low: number;
    high: number;
    unsigned: boolean;
};
type VoteAggregation = {
    name: string;
    voters: string[];
};
type ResponseAggregation = {
    response: string;
    responders: string[];
};
/** Infer the originating client family from WhatsApp's message-id format. */
export declare const getDevice: (id: string) => "android" | "desktop" | "ios" | "unknown" | "web";
/**
 * Aggregate votes from a poll message.
 *
 * Takes a poll creation message and its accumulated poll updates (from
 * `messages.update` events) and returns the vote tally per option.
 *
 * `pollUpdates` should contain pre-decrypted votes where `vote.selectedOptions`
 * are the SHA-256 hashes of the voted option names.
 *
 * @example
 * ```ts
 * sock.ev.on('messages.update', event => {
 *   for (const { key, update } of event) {
 *     if (update.pollUpdates) {
 *       const pollMsg = await getMessageFromStore(key)
 *       const votes = getAggregateVotesInPollMessage({
 *         message: pollMsg.message,
 *         pollUpdates: pollMsg.pollUpdates
 *       })
 *       console.log(votes) // [{ name: 'Yes', voters: ['jid1'] }, ...]
 *     }
 *   }
 * })
 * ```
 */
export declare function getAggregateVotesInPollMessage({ message, pollUpdates }: Pick<WAMessage, 'pollUpdates' | 'message'>, meId?: string): VoteAggregation[];
export declare function getAggregateResponsesInEventMessage({ eventResponses }: Pick<WAMessage, 'eventResponses'>, meId?: string): ResponseAggregation[];
/** Group non-self message keys by chat and participant for bulk receipts. */
export declare const aggregateMessageKeysNotFromMe: (keys: WAMessageKey[]) => {
    jid: string;
    participant: string | undefined;
    messageIds: string[];
}[];
/** Upsert one receipt into a message's `userReceipt` collection. */
export declare const updateMessageWithReceipt: (msg: Pick<WAMessage, 'userReceipt'>, receipt: MessageUserReceipt) => void;
/** Replace the previous reaction from the same author, then append the latest. */
export declare const updateMessageWithReaction: (msg: Pick<WAMessage, 'reactions'>, reaction: proto.IReaction) => void;
/** Replace the previous poll update from the same author. Empty votes remove it. */
export declare const updateMessageWithPollUpdate: (msg: Pick<WAMessage, 'pollUpdates'>, update: proto.IPollUpdate) => void;
/** Replace the previous event response from the same author. */
export declare const updateMessageWithEventResponse: (msg: Pick<WAMessage, 'eventResponses'>, update: proto.IEventResponse) => void;
/** Checks whether the message contains one of the upstream media payloads. */
export declare const assertMediaContent: (content: proto.IMessage | null | undefined) => WAProto.Message.IAudioMessage | WAProto.Message.IDocumentMessage | WAProto.Message.IImageMessage | WAProto.Message.IStickerMessage | WAProto.Message.IVideoMessage;
export {};
//# sourceMappingURL=messages.d.ts.map