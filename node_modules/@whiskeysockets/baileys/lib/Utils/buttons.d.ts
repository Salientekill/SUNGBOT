/**
 * Utilitários para enviar mensagens com botões no WhatsApp
 *
 * Baseado em análise de mensagens que funcionam em Android
 */
import { proto } from '../../WAProto/index.js';
export interface ButtonData {
    /** ID único do botão (será retornado quando clicado) */
    id: string;
    /** Texto exibido no botão */
    text: string;
}
export interface ButtonMessageOptions {
    /** Texto principal da mensagem */
    text: string;
    /** Texto do rodapé (footer) */
    footer?: string;
    /** Array de botões (máx. 3) */
    buttons: ButtonData[];
    /** Imagem já processada (imageMessage) */
    headerImage?: proto.Message.IImageMessage;
    /** Vídeo já processado (videoMessage) */
    headerVideo?: proto.Message.IVideoMessage;
    /** Documento já processado */
    headerDocument?: proto.Message.IDocumentMessage;
}
export interface TemplateButtonData {
    /** Tipo do botão */
    type: 'reply' | 'url' | 'call';
    /** Texto exibido */
    text: string;
    /** ID para reply, URL para url, número para call */
    value: string;
}
export interface TemplateMessageOptions {
    /** Texto principal */
    text: string;
    /** Rodapé */
    footer?: string;
    /** Título (header) */
    title?: string;
    /** Botões do template (máx. 3) */
    buttons: TemplateButtonData[];
    /** Imagem já processada */
    headerImage?: proto.Message.IImageMessage;
}
export interface ListSection {
    title: string;
    rows: Array<{
        id: string;
        title: string;
        description?: string;
    }>;
}
export interface ListMessageOptions {
    /** Texto principal */
    text: string;
    /** Título do botão que abre a lista */
    buttonText: string;
    /** Título da mensagem */
    title?: string;
    /** Rodapé */
    footer?: string;
    /** Seções da lista */
    sections: ListSection[];
}
/**
 * Cria uma mensagem com botões simples
 * IMPORTANTE: Para funcionar no Android, use com headerImage
 */
export declare function createButtonMessage(options: ButtonMessageOptions): proto.IMessage;
/**
 * Cria uma mensagem com botões de template (URL, Call, Reply)
 */
export declare function createTemplateMessage(options: TemplateMessageOptions): proto.IMessage;
/**
 * Cria uma mensagem de lista (menu)
 */
export declare function createListMessage(options: ListMessageOptions): proto.IMessage;
/**
 * Cria uma mensagem interativa com NativeFlow (formato mais recente)
 * Este é o formato usado pelo WhatsApp Business API
 */
export declare function createInteractiveMessage(options: {
    body: string;
    footer?: string;
    header?: {
        title?: string;
        subtitle?: string;
        imageUrl?: string;
    };
    buttons: Array<{
        name: string;
        buttonParamsJson: string;
    }>;
    viewOnce?: boolean;
}): proto.IMessage;
/**
 * Cria botão de copiar código (estilo OTP)
 * Este formato é usado por serviços de verificação
 */
export declare function createCopyCodeButton(options: {
    text: string;
    footer?: string;
    code: string;
    buttonText?: string;
    viewOnce?: boolean;
}): proto.IMessage;
declare const _default: {
    createButtonMessage: typeof createButtonMessage;
    createTemplateMessage: typeof createTemplateMessage;
    createListMessage: typeof createListMessage;
    createInteractiveMessage: typeof createInteractiveMessage;
    createCopyCodeButton: typeof createCopyCodeButton;
};
export default _default;
//# sourceMappingURL=buttons.d.ts.map