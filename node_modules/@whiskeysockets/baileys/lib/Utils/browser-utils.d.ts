import type { BrowsersMap } from '../Types/index.js';
export declare const Browsers: BrowsersMap;
export declare const getPlatformId: (browser: string) => string;
/**
 * Returns a random user-agent from the pool.
 * Use this for HTTP requests to avoid static fingerprinting.
 */
export declare const getRandomUserAgent: () => string;
/**
 * Returns a random browser configuration for anti-detection.
 * Each connection will have a different browser fingerprint.
 *
 * @returns Browser tuple [OS, Browser, Version]
 */
export declare const getRandomBrowser: () => [string, string, string];
//# sourceMappingURL=browser-utils.d.ts.map