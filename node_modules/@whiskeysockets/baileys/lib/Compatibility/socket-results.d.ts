import type { BlocklistEntryResult, BusinessProfileResult, MembershipRequestResult, ParticipantChangeResult } from '@oxidezap/whatsapp-rust-bridge';
import type { BinaryNode, WABusinessProfile } from '../Types/index.js';
export type ParticipantUpdateResult = {
    status: string;
    jid: string | undefined;
    content: BinaryNode;
};
export type MembershipRequestUpdateResult = {
    status: string;
    jid: string | undefined;
};
/** Translate the neutral blocklist result without exposing transport metadata. */
export declare const bridgeBlocklistToBaileys: (entries: readonly BlocklistEntryResult[]) => Array<string | undefined>;
/** Rebuild the public participant node from the bridge's typed response. */
export declare const bridgeParticipantChangeToBaileys: (result: ParticipantChangeResult) => ParticipantUpdateResult;
export declare const bridgeParticipantChangesToBaileys: (results: readonly ParticipantChangeResult[]) => ParticipantUpdateResult[];
export declare const bridgeMembershipRequestUpdatesToBaileys: (results: readonly ParticipantChangeResult[]) => MembershipRequestUpdateResult[];
/** Preserve the wire attribute names and string values exposed by the public socket. */
export declare const bridgeMembershipRequestsToBaileys: (results: readonly MembershipRequestResult[]) => Array<Record<string, string>>;
/** The bridge exposes a full invite URL; the public socket exposes only its code. */
export declare const bridgeInviteLinkToCode: (value: string | null | undefined) => string | undefined;
/**
 * Translate the neutral business-profile DTO at the public compatibility
 * boundary. The lower layers retain their own field naming and richer category
 * collection; the public socket exposes the established single-category shape.
 */
export declare const bridgeBusinessProfileToBaileys: (profile: BusinessProfileResult | null | undefined) => WABusinessProfile | undefined;
//# sourceMappingURL=socket-results.d.ts.map