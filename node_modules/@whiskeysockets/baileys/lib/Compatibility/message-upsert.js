/** Emit one canonical public message-upsert payload from any socket path. */
export const emitMessageUpsert = (ctx, messages, metadata) => {
    const payload = { messages, type: metadata.type };
    if (metadata.requestId)
        payload.requestId = metadata.requestId;
    ctx.ev.emit('messages.upsert', payload);
};
//# sourceMappingURL=message-upsert.js.map