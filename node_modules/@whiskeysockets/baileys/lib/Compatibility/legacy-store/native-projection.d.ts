import type { JsStoreCallbacks } from '@oxidezap/whatsapp-rust-bridge';
import type { SignalCreds, SignalKeyStore } from '../../Types/index.js';
/**
 * Project a neutral native byte store as the public legacy key store. Values
 * and key names reuse the same codecs/routing table as `wrapLegacyStore`.
 */
export declare const projectNativeStore: (store: JsStoreCallbacks, creds?: SignalCreds) => SignalKeyStore;
//# sourceMappingURL=native-projection.d.ts.map