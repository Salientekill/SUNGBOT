import { Buffer } from 'node:buffer';
export declare const EMPTY_BYTES: Uint8Array<ArrayBuffer>;
export declare const toJsonBytes: (value: unknown) => Uint8Array;
export declare const fromJsonBytes: (value: Uint8Array) => unknown;
export declare const toBytes: (value: unknown) => Uint8Array | null;
export declare const bytesToNumbers: (value: Uint8Array | Buffer) => number[];
export type NativeEnvelope = {
    payload: Uint8Array;
    legacyDigest: Buffer | null;
    nativeOnly: boolean;
};
/** Persist exact native bytes together with the projection generation. */
export declare function encodeNativeEnvelope(payload: Uint8Array, legacyValue: unknown): Buffer;
/**
 * Persist canonical bytes when the legacy model cannot represent their state.
 * The legacy fingerprint still detects a later write from another owner.
 */
export declare function encodeNativeOnlyEnvelope(payload: Uint8Array, legacyValue: unknown): Buffer;
/**
 * A non-envelope value is accepted as an old exact mirror and revalidated.
 * This makes the storage format forward-compatible with early local builds.
 */
export declare function decodeNativeEnvelope(value: unknown): NativeEnvelope | null;
export declare function envelopeMatchesLegacy(envelope: NativeEnvelope, legacyValue: unknown): boolean;
//# sourceMappingURL=common.d.ts.map