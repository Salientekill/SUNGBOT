/**
 * Small LRU+TTL map used only by the standalone Baileys-compatible retry
 * manager. The connected client's retry cache remains entirely in Rust.
 */
export class ExpiringLruMap {
    constructor(options) {
        this.entries = new Map();
        this.options = options;
    }
    set(key, value) {
        this.delete(key);
        const entry = {
            value,
            expiresAt: Date.now() + this.options.ttl
        };
        entry.timer = setTimeout(() => {
            if (this.entries.get(key) === entry)
                this.delete(key);
        }, this.options.ttl);
        entry.timer.unref?.();
        this.entries.set(key, entry);
        const max = this.options.max;
        if (max !== undefined && this.entries.size > max) {
            const oldest = this.entries.keys().next();
            if (!oldest.done)
                this.delete(oldest.value);
        }
    }
    get(key) {
        const entry = this.entries.get(key);
        if (!entry)
            return undefined;
        if (entry.expiresAt <= Date.now()) {
            this.delete(key);
            return undefined;
        }
        this.entries.delete(key);
        this.entries.set(key, entry);
        if (this.options.updateAgeOnGet)
            this.refreshTimer(key, entry);
        return entry.value;
    }
    delete(key) {
        const entry = this.entries.get(key);
        if (!entry)
            return false;
        this.entries.delete(key);
        if (entry.timer)
            clearTimeout(entry.timer);
        this.options.dispose?.(entry.value, key);
        return true;
    }
    clear() {
        for (const key of this.entries.keys())
            this.delete(key);
    }
    refreshTimer(key, entry) {
        if (entry.timer)
            clearTimeout(entry.timer);
        entry.expiresAt = Date.now() + this.options.ttl;
        entry.timer = setTimeout(() => {
            if (this.entries.get(key) === entry)
                this.delete(key);
        }, this.options.ttl);
        entry.timer.unref?.();
    }
}
//# sourceMappingURL=expiring-lru-map.js.map