// A WeakSet keeps the native-backend hint private to the compatibility
// boundary: it neither changes the public store object nor crosses into WASM.
const nativeMemoryStores = new WeakSet();
export const markNativeMemoryStore = (store) => {
    nativeMemoryStores.add(store);
};
export const isNativeMemoryStore = (store) => nativeMemoryStores.has(store);
//# sourceMappingURL=native-memory-store.js.map