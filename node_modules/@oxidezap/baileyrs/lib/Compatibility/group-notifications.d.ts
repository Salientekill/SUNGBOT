import type { CanonicalGroupParticipant, CanonicalGroupUpdate } from '../Bridge/types.js';
import type { BaileysEventMap, ChatUpdate, GroupMetadata, GroupParticipant, WAMessage } from '../Types/index.js';
/** Match the object built from a `<participant>` by upstream messages-recv. */
export declare const canonicalGroupParticipantToBaileys: (participant: CanonicalGroupParticipant) => GroupParticipant;
export type GroupNotificationDomainEvent = {
    name: 'group-participants.update';
    payload: BaileysEventMap['group-participants.update'];
} | {
    name: 'groups.update';
    payload: BaileysEventMap['groups.update'];
} | null;
/** Build the public join-request events, one per affected participant. */
export declare const buildGroupJoinRequestEvents: (notification: CanonicalGroupUpdate) => BaileysEventMap['group.join-request'][];
/** Build the public high-level event derived by upstream process-message. */
export declare const buildGroupNotificationDomainEvent: (notification: CanonicalGroupUpdate) => GroupNotificationDomainEvent;
/** Chat updates that upstream process-message derives while handling stubs. */
export declare const buildGroupNotificationChatUpdates: (notification: CanonicalGroupUpdate) => ChatUpdate[];
/** Build the upstream-shaped notification messages for one canonical action. */
export declare const buildGroupNotificationStubMessages: (notification: CanonicalGroupUpdate, fromMe: boolean) => WAMessage[];
/** Build the create stub after metadata has been resolved. */
export declare const buildGroupCreateStubMessage: (notification: CanonicalGroupUpdate, metadata: GroupMetadata, fromMe: boolean) => WAMessage;
//# sourceMappingURL=group-notifications.d.ts.map