import type { WAMessageKey } from '../Types/index.js';
export type ReceiptMessageKey = {
    remoteJid: string;
    id: string;
    participant?: string;
};
/** Project public message keys to the validated receipt shape used by the transport. */
export declare const receiptMessageKeys: (keys: WAMessageKey[]) => ReceiptMessageKey[];
//# sourceMappingURL=message-keys.d.ts.map