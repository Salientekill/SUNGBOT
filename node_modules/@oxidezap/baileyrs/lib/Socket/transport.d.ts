import type { JsHttpClientConfig, JsTransportCallbacks } from 'whatsapp-rust-bridge';
import type { ILogger } from '../Utils/logger.js';
interface TransportConfig {
    waWebSocketUrl: string | URL;
    logger: ILogger;
    /** RequestInit options passed to fetch() — use `dispatcher` for proxy/TLS config */
    options?: RequestInit;
}
export declare const makeTransport: (config: TransportConfig) => JsTransportCallbacks;
export declare const makeHttpClient: (config: TransportConfig) => JsHttpClientConfig;
export {};
//# sourceMappingURL=transport.d.ts.map