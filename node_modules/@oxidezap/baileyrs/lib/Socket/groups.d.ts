import type { GroupMetadata } from '../Types/index.js';
import type { SocketContext } from './types.js';
export declare const makeGroupMethods: (ctx: SocketContext) => {
    groupMetadata: (jid: string) => Promise<GroupMetadata>;
    groupCreate: (subject: string, participants: string[]) => Promise<import("whatsapp-rust-bridge").CreateGroupResult>;
    groupLeave: (jid: string) => Promise<void>;
    groupUpdateSubject: (jid: string, subject: string) => Promise<void>;
    groupUpdateDescription: (jid: string, description?: string) => Promise<void>;
    groupParticipantsUpdate: (jid: string, participants: string[], action: "add" | "remove" | "promote" | "demote") => Promise<import("whatsapp-rust-bridge").ParticipantChangeResult[]>;
    groupFetchAllParticipating: () => Promise<Record<string, GroupMetadata>>;
    groupInviteCode: (jid: string) => Promise<string>;
    groupRevokeInvite: (jid: string) => Promise<string>;
    groupSettingUpdate: (jid: string, setting: "locked" | "announce" | "membership_approval" | "announcement" | "not_announcement" | "unlocked" | "on" | "off", value?: boolean) => Promise<void>;
    groupToggleEphemeral: (jid: string, expiration: number) => Promise<void>;
    groupAcceptInvite: (code: string) => Promise<string | undefined>;
    /** Join a group via a GroupInviteMessage (V4 invite). */
    groupAcceptInviteV4: (key: {
        remoteJid?: string | null;
    }, msg: {
        inviteCode?: string | null;
        inviteExpiration?: number | null;
        groupJid?: string | null;
    }) => Promise<string | undefined>;
    groupGetInviteInfo: (code: string) => Promise<GroupMetadata>;
    groupRequestParticipantsList: (jid: string) => Promise<import("whatsapp-rust-bridge").MembershipRequestResult[]>;
    groupRequestParticipantsUpdate: (jid: string, participants: string[], action: "approve" | "reject") => Promise<void>;
    /**
     * Set or clear the bot's per-group "member label" — the small tag rendered
     * under the bot's display name inside that group's UI. Empty `label`
     * clears it. Sent as a `ProtocolMessage` over the regular message path
     * (matching WA Web's wire format), not as an IQ.
     *
     * Self-applied only — WhatsApp's protocol does not let admins change
     * other members' labels even with admin privileges; the same restriction
     * applies in the official mobile app.
     */
    updateMemberLabel: (jid: string, label: string) => Promise<void>;
};
//# sourceMappingURL=groups.d.ts.map