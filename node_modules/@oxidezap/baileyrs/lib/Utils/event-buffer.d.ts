import type { BaileysEventEmitter } from '../Types/index.js';
/**
 * Simple event emitter with `process()` support for Baileys events.
 * The bridge handles event ordering internally — no buffering needed.
 */
export declare const makeEventBuffer: () => BaileysEventEmitter;
//# sourceMappingURL=event-buffer.d.ts.map